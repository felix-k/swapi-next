import { Divider } from "@mui/material";
import { SearchBar } from "@/src/shared/ui";

const SearchToolbar = () => {
  return (
    <>
      <Divider />
      <SearchBar />
      <Divider />
    </>
  );
};

export default SearchToolbar;
